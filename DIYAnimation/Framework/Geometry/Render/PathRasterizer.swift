import CoreGraphics.CGPath

/// Uses a scan rasterization technique to convert a `CGPath` into a renderable
/// shape. Maintains a grid for tiled rasterization requests.
internal struct PathRasterizer {
    
    // TODO
    
}
